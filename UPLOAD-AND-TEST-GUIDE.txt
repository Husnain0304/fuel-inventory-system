STOCK RESERVATIONS & AVAILABLE-TO-PROMISE V1

Upload complete app.py, stock_reservations.py and rbac.py to the root of foundation-v2. Wait for Streamlit redeployment; reboot once only if the menu remains old. Never reset the database.

TEST
1. Open Reservations & ATP as Admin.
2. Availability must show usable released stock, active reservations and available-to-promise.
3. Create a small reservation for an existing product. Confirm physical inventory does not change and ATP reduces.
4. Try a request larger than ATP with partial reservation disabled: it must be blocked.
5. Enable partial reservation: it must reserve only the available quantity.
6. Cancel a test request with a reason: outstanding quantity must return to ATP.
7. Post a small OUT transaction in Storage Operations.
8. Return to Fulfil reservation and link that existing STX transaction.
9. Confirm no new movement is created and status becomes PARTIALLY_FULFILLED or FULFILLED.
10. Download the ATP & demand report and check all three worksheets.
11. Search Audit Centre for CREATE_RESERVATION, CANCEL_RESERVATION and FULFIL_RESERVATION.
